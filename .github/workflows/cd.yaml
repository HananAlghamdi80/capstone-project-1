name: Continuous Deployment

on:
  push:
    branches:
      - main  # This workflow runs on every push to the main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        server_ip: [54.210.13.167, 54.162.59.154]  # Replace with your actual server IPs

    steps:
      - name: Checkout code
        uses: actions/checkout@v2  # Checkout the code from the repository

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}  # Docker Hub username
          password: ${{ secrets.DOCKER_PASSWORD }}  # Docker Hub password

      - name: Pull Docker image
        run: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/capstone-project:latest  # Pull the Docker image

      - name: Deploy to Server
        run: |
          scp -i ~/capstone-project-Hanan/pem.pem -o StrictHostKeyChecking=no docker-compose.yml ec2-user@${{ matrix.server_ip }}:/home/ec2-user/  # Copy docker-compose to the server
          ssh -i ~/capstone-project-Hanan/pem.pem -o StrictHostKeyChecking=no ec2-user@${{ matrix.server_ip }} "docker-compose up -d"  # Run Docker Compose on the server
